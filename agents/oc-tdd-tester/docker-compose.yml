version: '3.8'

services:
  oc-tdd-tester:
    image: node:20-slim
    container_name: oc-tdd-tester
    hostname: oc-tdd-tester
    
    environment:
      - AGENT_NAME=oc-tdd-tester
      - AGENT_ROLE=tdd-tester
      - AGENT_PHASE=RED
      - OPENCLAW_STATE_DIR=/home/agent/.openclaw
      - SHARED_DIR=/shared
      - REDIS_URL=redis://redis:6379
    
    volumes:
      # Agent 工作空间
      - ./workspace:/home/agent/workspace
      - ./data:/home/agent/.openclaw
      - ./config:/home/agent/config:ro
      - ./logs:/home/agent/logs
      # 共享目录
      - ../../shared:/shared:rw
    
    working_dir: /home/agent/workspace
    
    networks:
      - agent-network
    
    command: >
      bash -c "
        echo 'oc-tdd-tester is ready' &&
        tail -f /dev/null
      "
    
    restart: unless-stopped
    
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G

networks:
  agent-network:
    external: true
