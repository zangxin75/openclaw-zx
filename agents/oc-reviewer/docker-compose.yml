version: '3.8'

services:
  oc-reviewer:
    image: node:20-slim
    container_name: oc-reviewer
    hostname: oc-reviewer
    
    environment:
      - AGENT_NAME=oc-reviewer
      - AGENT_ROLE=reviewer
      - OPENCLAW_STATE_DIR=/home/agent/.openclaw
      - SHARED_DIR=/shared
      - REDIS_URL=redis://redis:6379
    
    volumes:
      - ./workspace:/home/agent/workspace
      - ./data:/home/agent/.openclaw
      - ./config:/home/agent/config:ro
      - ./logs:/home/agent/logs
      - ../../shared:/shared:rw
    
    working_dir: /home/agent/workspace
    
    networks:
      - agent-network
    
    command: >
      bash -c "
        echo 'oc-reviewer is ready' &&
        tail -f /dev/null
      "
    
    restart: unless-stopped
    
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G

networks:
  agent-network:
    external: true
