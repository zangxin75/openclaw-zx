version: '3.8'

services:
  oc-ops:
    image: node:20-slim
    container_name: oc-ops
    hostname: oc-ops
    
    environment:
      - AGENT_NAME=oc-ops
      - AGENT_ROLE=ops
      - OPENCLAW_STATE_DIR=/home/agent/.openclaw
      - SHARED_DIR=/shared
      - REDIS_URL=redis://redis:6379
    
    volumes:
      - ./workspace:/home/agent/workspace
      - ./data:/home/agent/.openclaw
      - ./config:/home/agent/config:ro
      - ./logs:/home/agent/logs
      - ../../shared:/shared:rw
      # Docker socket for container management
      - /var/run/docker.sock:/var/run/docker.sock:ro
    
    working_dir: /home/agent/workspace
    
    networks:
      - agent-network
    
    command: >
      bash -c "
        echo 'oc-ops is ready' &&
        tail -f /dev/null
      "
    
    restart: unless-stopped
    
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G

networks:
  agent-network:
    external: true
