version: '3.8'

services:
  oc-tdd-coder:
    image: node:20-slim
    container_name: oc-tdd-coder
    hostname: oc-tdd-coder
    
    environment:
      - AGENT_NAME=oc-tdd-coder
      - AGENT_ROLE=tdd-coder
      - AGENT_PHASE=GREEN
      - OPENCLAW_STATE_DIR=/home/agent/.openclaw
      - SHARED_DIR=/shared
      - REDIS_URL=redis://redis:6379
    
    volumes:
      - ./workspace:/home/agent/workspace
      - ./data:/home/agent/.openclaw
      - ./config:/home/agent/config:ro
      - ./logs:/home/agent/logs
      - ../../shared:/shared:rw
    
    working_dir: /home/agent/workspace
    
    networks:
      - agent-network
    
    command: >
      bash -c "
        echo 'oc-tdd-coder is ready' &&
        tail -f /dev/null
      "
    
    restart: unless-stopped
    
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G

networks:
  agent-network:
    external: true
